org.gradle.vfs.watch=true
org.gradle.vfs.verbose=true

# Don't auto-add kotlin-stdlib to api/implementation. We declare it explicitly per-module
# so consumers aren't forced onto our compile-time Kotlin version.
kotlin.stdlib.default.dependency=false